# train and val data as 1) directory: path/images/, 2) file: path/images.txt, or 3) list: [path1/images/, path2/images/]
train: /kaggle/working/esod/train.txt  # 4K images
val: /kaggle/working/esod/val.txt  # 700 imageS
#test: /kaggle/working/esod/val.txt  # 20288 of 40670 images, submit to https:/competitions.codalab.org/competitions/20794

# number of classes
nc: 1

# class names
# names: ['person']
names: ['orange']

# Print classes
# with open('data/coco.yaml') as f:
#   d = yaml.load(f, Loader=yaml.FullLoader)  # dict
#   for i, x in enumerate(d['names']):
#     print(i, x)
